<%- include('partials/logged-in-nav') -%>
<div class="col mt-3">
  <!-- <div class="d-flex flex-row p-2"> -->
      
<div class="container mt-0">
  <div class="row mt-0">
    <div class="col">
      <ul class="row list-unstyled">
        <% for(var i=0; i<project.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <a href="/project/<%= project[i]._id%>">
              <p class="" src="<%= project[i].assignee%>">
            </a>
          </li>
        <% } %>
        </ul>
    </div>

    <div class="col col-md-2 justify-content-end align-items-end">
        <a href="/assignee" class="text-decoration-none fw-bold text-dark"><p> <%= project.assignee %></p></a>
        <a href="/logout" class="col-7 btn btn-secondary">Logout</a>
    </div>
  </div> 
</div>
</div>
<%- include('partials/footer') -%>

<!-- Need to grab the assignee Id number and push them into an array. Then a card needs to be created for every item in the array that includes all the information 
from that card -->